<% if order.apply_credit and order.store_credit_amount > 0 %>
    <li class="pull-left">Your Bonus Credit</li>
    <li class="pull-right"><%= number_to_currency(order.store_credit_amount) %></li>
    <br/>
    <%= form.hidden_field :apply_credit %>
    <%= form.hidden_field :store_credit_amount %>
<% end %>

 <%# This block is from default store credits. Commented now but can be used in the future  %>
<% if false %>
<% if (current_user && current_user.store_credits_total > 0) %>
<br style='clear:both;' />
<p><%= t('you_have_store_credit',
       :amount => number_to_currency(current_user.store_credits_total))%>
</p>
<p>
  <label><%= t('enter_desired_amount_of_store_credit') %></label><br />
  <%= form.text_field :store_credit_amount, :size => 19 %>
</p>
<% end %>
<% end %>